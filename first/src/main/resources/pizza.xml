<?xml version="1.0" encoding="UTF-8" ?>
<flow xmlns="http://www.springframework.org/schema/webflow"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:schemaLocation="http://www.springframework.org/schema/webflow
        http://www.springframework.org/schema/webflow/spring-webflow-2.0.xsd">
    <var name="order" class="com.springinaction.pizza.domain.Order">

    <!-- Вызов последовательности идентификации клиента -->
    <subflow-state id="identifyCustomer" subflow="pizza/cusomter">
        <output name="customer" value="order.customer"/>
        <transition on="customerReady" to="buildOrder"/>
    </subflow-state>

    <!-- Вызов подпоследовательности оформления заказа -->
    <subflow-state id="buildOrder" subflow="pizza/order">
        <input name="order" value="order"/>
        <transition on="orderCreated" to="takePayment"/>
    </subflow-state>

    <!-- Вызов подпоследовательности приема оплаты -->
    <subflow-state id="takePayment" subflow="pizza/paymnet">
        <input name="order" value="order"/>
        <transition on="paymentTaken" to="saveOrder"/>
    </subflow-state>

    <!--Сохранить заказ-->
    <action-state id="saveOrder">
        <evaluate expression="pizzaFlowActions.saveOrder(order)"/>
        <transition to="thankCustomer"/>
    </action-state>

    <!-- Выражение благодарности клиенту-->
    <view-state id="thankCustomer">
        <transition to="endState"/>
    </view-state>

    <end-state id="endState"/>

    <!-- Глобальный переход в случае отмены заказа -->
    <global-transitions>
        <transition on="cancel" to="endState"/>
    </global-transitions>
</flow>